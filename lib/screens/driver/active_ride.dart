
import 'package:flutter/material.dart';

class ActiveRideContent extends StatelessWidget {
  const ActiveRideContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}
