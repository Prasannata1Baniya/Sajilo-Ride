
import 'package:flutter/material.dart';

class CarManagementContent extends StatelessWidget {
  const CarManagementContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
