
import 'package:flutter/material.dart';

class EarningContent extends StatelessWidget {
  const EarningContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
