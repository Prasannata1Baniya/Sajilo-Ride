import 'package:flutter/material.dart';

class BookingConfirmContent extends StatelessWidget {
  const BookingConfirmContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
