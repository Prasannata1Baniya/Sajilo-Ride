import 'package:flutter/material.dart';

class CarDriverDetail extends StatelessWidget {
  const CarDriverDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
